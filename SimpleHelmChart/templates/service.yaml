apiVersion: v1
kind: Service
metadata:
  name: {{ include "SimpleHelmChart.fullname" . }}
  labels:
    {{- include "SimpleHelmChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "SimpleHelmChart.selectorLabels" . | nindent 4 }}
